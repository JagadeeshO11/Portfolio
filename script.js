const glow=document.querySelector('.cursor-glow');
window.addEventListener('pointermove',e=>{if(glow){glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'}});
document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const el=document.querySelector(a.getAttribute('href'));if(el){e.preventDefault();el.scrollIntoView({behavior:'smooth',block:'start'})}}));
const reduceMotion=window.matchMedia('(prefers-reduced-motion: reduce)').matches;
if(!reduceMotion){
 const style=document.createElement('style');style.textContent=`.reveal{opacity:0;transform:translateY(32px);transition:opacity .7s ease,transform .7s cubic-bezier(.2,.7,.2,1)}.reveal.is-visible{opacity:1;transform:none}.reveal.delay-1{transition-delay:.12s}.reveal.delay-2{transition-delay:.24s}.scroll-progress{position:fixed;top:0;left:0;width:100%;height:3px;transform-origin:left center;transform:scaleX(0);z-index:99999;background:#71f5bf;box-shadow:0 0 14px rgba(113,245,191,.7);pointer-events:none}`;document.head.appendChild(style);
 const progress=document.createElement('div');progress.className='scroll-progress';document.body.appendChild(progress);const updateProgress=()=>{const max=document.documentElement.scrollHeight-window.innerHeight;progress.style.transform=`scaleX(${max>0?window.scrollY/max:0})`};window.addEventListener('scroll',updateProgress,{passive:true});window.addEventListener('resize',updateProgress,{passive:true});updateProgress();
 const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{threshold:.08,rootMargin:'0px 0px -40px 0px'});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
 const role=document.querySelector('.typing-role'),text=role?.querySelector('.typing-text');if(role&&text){const roles=['Full Stack Developer','React Developer','AI / IoT Builder','Problem Solver'];let ri=0,ci=0,del=false;const type=()=>{const word=roles[ri];text.textContent=word.slice(0,ci);if(!del){if(ci<word.length){ci++;setTimeout(type,68)}else{del=true;setTimeout(type,1450)}}else{if(ci>0){ci--;setTimeout(type,38)}else{del=false;ri=(ri+1)%roles.length;setTimeout(type,300)}}};type()}
 document.querySelectorAll('[data-tilt]').forEach(card=>{const max=Number(card.dataset.tiltMax||6);card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${(-y*max).toFixed(2)}deg) rotateY(${(x*max).toFixed(2)}deg) translateZ(4px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});
 const marquee=document.querySelector('[data-stack-marquee]'),track=marquee?.querySelector('.stack-track');if(track){const originals=[...track.children];originals.forEach(c=>track.appendChild(c.cloneNode(true)));originals.slice().reverse().forEach(c=>track.insertBefore(c.cloneNode(true),track.firstChild));let offset=0,last=performance.now(),cycle=0,paused=false;const measure=()=>{const cards=[...track.children];cycle=cards[originals.length].getBoundingClientRect().left-cards[0].getBoundingClientRect().left;offset=-cycle};const focus=()=>{const center=marquee.getBoundingClientRect().left+marquee.clientWidth/2;[...track.children].forEach(card=>{const r=card.getBoundingClientRect(),d=Math.abs(r.left+r.width/2-center),m=Math.max(180,marquee.clientWidth*.43),i=Math.max(0,1-d/m);card.style.transform=`translate3d(0,${-i*18}px,0) scale(${.72+i*.32}) rotateY(${((r.left+r.width/2-center)/m*-9)}deg)`;card.style.opacity=.28+i*.72;card.style.filter=`saturate(${.55+i*.55}) blur(${Math.max(0,(d-170)/110).toFixed(2)}px)`})};marquee.addEventListener('mouseenter',()=>paused=true);marquee.addEventListener('mouseleave',()=>paused=false);const animate=now=>{const dt=Math.min(40,now-last);last=now;if(!paused){offset+=dt*.032;if(cycle&&offset>=0)offset-=cycle;if(cycle&&offset<-cycle)offset+=cycle;track.style.transform=`translate3d(${offset}px,0,0)`}focus();requestAnimationFrame(animate)};window.addEventListener('resize',()=>{cycle=0;requestAnimationFrame(()=>{measure();focus()})});requestAnimationFrame(()=>{measure();focus();requestAnimationFrame(animate)})}
}
